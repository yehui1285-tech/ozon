# 真实浏览器验收清单

用途：每次升级扩展（重新生成 `ozon-erp-collector-extension.zip`）或修改网页后，在真实 Chrome/Edge 里按本清单逐项验收，再把结果回写 `CHANGELOG.md` 对应版本的「验证」栏。

背景：自动化测试（`npm test`）只覆盖纯解析/规则/Worker 逻辑，无法覆盖真实浏览器交互与 Ozon / 毛子 ERP 动态页面的加载行为——这正是历次版本里反复出现「真实浏览器验收待做」的原因。本清单把这些缺口固化成可重复执行的步骤。

更新时间：2026-08-27

## 0. 准备

- [ ] Chrome 打开 `chrome://extensions/`（Edge 为 `edge://extensions/`），开启「开发者模式」。
- [ ] 删除旧版 `OZON ERP Detail Sender`，重新加载 `ozon-erp-collector-extension` 文件夹（分发时先解压最新 zip 再加载）。
- [ ] 确认扩展详情页版本号与本次交付一致（当前 `0.6.20`）。

## 1. 商品详情页采集（核心闭环）

- [ ] 打开任一 Ozon 商品详情页，等毛子 ERP 插件加载完成。
- [ ] 点扩展图标 →「启动当前页采集」，左侧出现「OZON 详情采集」面板。
- [ ] 点「检查本页数据」，确认 SKU、绿标价格、佣金、尺寸、重量、跟卖最低价均正确识别。
- [ ] 修改绿标价格后，佣金 / 国际运费按当前规则自动重算。
- [ ] 点「发送到核价页」，核价页新增一行、不覆盖上一行。

## 1.1 黑标价自动读取（0.6.11）

- [ ] 找一个“页面绿底价 ≤ 跟卖最低价”的商品，点“检查本页数据”，确认黑标价自动读取当前页原始黑价，不是毛子 ERP 公式黑标标签。
- [ ] 找一个“跟卖最低价 < 页面绿底价”的商品，确认扩展从跟卖列表定位对应商品，后台临时打开该商品页并回填同源原始黑价，完成后临时页自动关闭。
- [x] 回归SKU `4984098622`：确认不会再误选灰色“跟卖列表：”标签，能从“Крупные ...等9个卖家”浮层取得最低价商品链接。（2026-08-25用户验收通过）
- [x] 同一回归SKU确认最低价商品SKU为`5382620664`、自动回填原始黑价`266.36`；用户确认读取与回填表现完美，具体秒数未单独记录。（2026-08-25）
- [ ] 自动结果出现前手工填写黑标价，确认自动结果不会覆盖手工值。
- [ ] 在跟卖弹窗或黑价无法读取的场景，确认黑标价留空并显示手工填写提示，SKU、绿标、佣金、尺寸、重量和发送流程仍可使用。
- [ ] 发送到核价页后，确认新增行的绿标价与黑标价来自同一商品来源，真实售价、定价、利润与利润率能正常计算。

## 1.2 找品任务主图补全（0.6.12）

- [ ] 点扩展图标 → “打开找品详情补全”，选择`batch-1786777412364-sample-10.json`，确认显示10件商品。
- [ ] 点“自动补齐主图”，确认每次只出现一个后台临时商品页，读到主图后自动关闭并继续下一件。
- [ ] 完成后确认成功数、失败数和单件耗时；表格成功项能显示对应商品主图，不是Ozon Logo、广告或推荐商品图。
- [ ] 点击“下载补齐后的JSON”，确认文件名带`-main-images.json`，原始样本JSON仍存在且没有被覆盖。
- [ ] 如有失败项，再次点击“自动补齐主图”，确认只重试没有主图的商品；记录失败SKU、错误提示和总耗时。

## 1.3 主图准确率优先回归（0.6.15）

- [x] 使用未补主图的`batch-1786777412364-sample-10-retest-0.6.15.json`完成真实运行，没有复用旧成功结果。
- [x] 成功10/10、失败0；全部读取方式为“标签页可靠模式”，来源均为`og:image`。
- [x] 平均3832毫秒、最短1506毫秒、最慢7078毫秒，优于0.6.12平均9561毫秒、最慢28814毫秒的基线。
- [x] 10个图片URL全部有效且唯一；与0.6.12成功结果逐SKU比对，10张完全一致。

## 1.4 Ozon任务批量核价补全（0.6.26，待验收）

- [ ] 在批量店铺扫描页点击“导出找品任务 JSON”，确认下载`Ozon找品任务_YYYY-MM-DD.json`，件数与页面“符合要求”总数一致。
- [ ] 导入该JSON，确认每件任务都有`qualified / batch_store_scan`资格且商品链接有效，无需再经过MD转换。
- [ ] 点击“一键补齐主图和核价”，确认同一商品先出现主图读取状态、再出现核价状态，当前件保存完成后才进入下一件；不得同时打开多个商品页。
- [ ] 对已有主图、缺核价的任务再次一键运行，确认跳过主图只补核价；反向情况同理。
- [ ] 中途点击“处理完当前件后停止”，确认当前商品的主图和核价阶段均结束后停止，刷新后可继续未完成商品。
- [ ] 导入带`migratedFromLegacyBatch:true`的旧JSON，确认该推断资格被撤销，不会直接绕过详情页标签检查。

- [ ] 导入含主图的任务JSON，确认页面显示绿标、黑标、国际运费、18%最高采购成本和线路。
- [ ] 点击“批量补齐Ozon核价”，确认每次只处理一件，并且同一任务最多使用一个后台标签页。
- [ ] 确认每件任务开始采集前，后台标签页地址中的商品SKU与当前任务SKU一致；不得再次出现10件任务都读取成同一个旧页面SKU。
- [ ] 导入由批量店铺扫描生成的标准任务JSON，确认旧JSON会自动补上`batch_store_scan`可信资格，核价时不再等待详情页选品标签。
- [ ] 对标签很晚才出现或本轮未出现的可信任务，确认仍直接采集价格与核价字段，不因标签缺失跳过。
- [ ] 导入不含批次、店铺来源的任意JSON时，确认不会自动获得可信资格，仍执行详情页资格校验。
- [ ] 对标题只显示图标但SKU、rFBS佣金、尺寸、重量和跟卖数据正常的毛子ERP面板，确认不会再误报“毛子ERP面板”缺失。
- [ ] 分别覆盖页面价胜出与跟卖最低价胜出；确认黑标来自绿标对应的同一商品页面。
- [ ] 用核价页复算至少2件，确认国际运费、佣金档位和18%最高采购成本一致；最高成本增加0.01元后利润率应低于18%。
- [ ] 中途刷新补全页，确认已完成结果恢复、当时运行项回到可重试状态，并从未完成项继续。
- [ ] 下载`*-ozon-pricing.json`，确认主图与核价都完整的任务进入`pending_pinduoduo_search`，缺任一类字段的任务仍为`pending_ozon_enrichment`。
- [ ] 使用`batch-1786777412364-sample-10-retest-0.6.15.json`回归：SKU `4379290049`必须等待并采用当前跟卖最低价（用户测试时为`266.44`，以复测实时页面为准），不能再次只取较高页面价。
- [ ] SKU `4702633521`不得复用JSON历史值`429.28`；如果当前页面没有该价格，结果中不得出现该值。
- [ ] SKU `5154673032`如核价失败，“来源/失败原因”必须显示具体原因，不能只显示“核价失败”。
- [ ] SKU `5154673032`回归：Ozon官方页面价以实时页面为准（2026-08-27检查为`265.22`），ERP跟卖最低价以实时面板为准（当时为`197.22`），最终绿标应取两者较低值；结果绝不能出现ERP利润`69.22`。
- [ ] 在网速慢、Ozon官方价格组件尚未加载时，确认任务等待或可重试失败，不会从毛子ERP利润、黑标标签或其它绿色文字生成价格。
- [ ] 导入带0.6.24及更早核价结果的JSON，确认旧成功、旧失败及旧“不合要求”均重新进入等待核价，不复用旧价格。

## 2. 佣金档位边界

- [ ] 规则确认：绿标 ≤600 取第 2 档，>600 取第 3 档，第 1 档不再使用。
- [ ] 回归样本：SKU `4479204333` → 绿标 `146.65` → 自动佣金 `17%`（第 2 档）。
- [ ] 回归样本：SKU `3871914086` → 绿标 `894.14` → 自动佣金 `18%`（第 3 档）。
- [ ] 边界复核：绿标临时改 `600` → 第 2 档；改 `600.01` → 第 3 档（复核后恢复原值）。

## 3. 核价页（线上 feishu.html）

- [ ] 打开 `https://yehui1285-tech.github.io/ozon/feishu.html?v=20260720`，确认版本标识正确。
- [ ] 采集行自动计算佣金、国际运费、定价、利润、利润率。
- [ ] 关闭或刷新页面后，记录自动恢复（本地自动保存）。
- [ ] 导出 CSV → 清空全部 → 导入 CSV，数据还原且公式按当前规则重算。

## 4. 飞书同步

- [ ] 五项必填完整时点「同步到飞书多维表格」，提示新增 / 跳过重复行数正确。
- [ ] 相同 SKU 或跟卖链接第二次同步被跳过。
- [ ] 「打开飞书多维表格」按钮能打开正确的多维表格。

## 5. 单店店铺扫描

- [ ] 打开 Ozon 店铺页（`/seller/.../`），启动扩展后左侧出现「OZON 店铺扫描」面板。
- [ ] 自动 / 手动滚动，只收录明确显示「符合要求」标签的商品，并按 SKU 去重。
- [ ] 到达「您可能喜欢」边界后停止，推荐区商品不计入结果。
- [ ] 导出 Markdown 含名称 / SKU / 价格 / 佣金 / 月销 / 发货模式 / 尺寸 / 重量 / 跟卖最低价 / 链接。

## 6. 批量店铺扫描（多店）

- [ ] 扩展图标 →「打开批量店铺扫描」，粘贴多家店铺（≤50），点「开始新任务」。
- [ ] 专用标签页按输入顺序扫描，单店失败自动重试 2 次、店铺间隔约 8 秒。
- [ ] 暂停 / 继续 / 停止 / 跳过 / 结束当前店均正常。
- [ ] 删除某行店铺（含正在扫描的店）不打断其余任务。
- [ ] 扫描中店铺页刷新后显示「刷新后恢复中」并自动续扫，本轮已见商品不重复计数。
- [ ] 对同一家加载困难店铺连续触发两次刷新：第 1 次自动恢复；第 2 次标为“已跳过”，保留已找到商品，并在约 8 秒后开始下一家。
- [ ] 「扫描动态」列显示阶段、连续无新增屏数、重载次数、疑似卡住提示。
- [ ] 一轮完成后导出汇总 Markdown / CSV。

## 7. 清空当前批次（0.6.7）

- [ ] 完成或停止一轮后点「清空当前批次」，确认二次弹窗。
- [ ] 链接输入、任务列表、四项统计、运行状态全部归零，光标回到输入框。
- [ ] 电脑里已下载的 Markdown / CSV 文件仍存在。
- [ ] 相同店铺历史采集记录仍可读取（批量页重新导出该店验证）。
- [ ] 运行中清空会停止当前店扫描、撤销切店计时与闹钟，旧页面迟到进度被拒绝。

## 8. 拼多多找品耗时埋点（MVP 5.1，待验收）

- [ ] 关闭旧拼多多 Agent 页面后重新双击`启动拼多多找品Agent.cmd`，导入一份至少3件的已完成Ozon核价任务JSON。
- [ ] 完成一件正常找同款后，确认“耗时埋点”面板出现该SKU的总耗时、平均/最慢摘要以及主图下载、MuMu下发、以图搜索、候选详情、证据图和返回搜索页等阶段。
- [ ] 确认候选详情加载慢或失败的任务仍显示失败记录及慢阶段；已取得商品ID时仍保留原有候选链接和具体缺失字段。
- [ ] 批量完成至少3件后，确认面板只显示最近6件、平均/最慢耗时合理，刷新页面后任务JSON中的耗时记录恢复。
- [ ] 确认新增记录没有改变三候选完整核验、候选切换1.2–2.6秒随机缓冲、批量12–25秒间隔、风控自动暂停或“不会提交订单”的行为。
- [ ] 记录最慢的两个阶段、每件总耗时和是否出现详情重试，再决定下一轮只优化有数据证实的瓶颈。

## 9. AI同款一致性与规格核验诊断（MVP 5.2，待验收）

- [ ] 关闭旧Agent页面和黑色启动窗口，重新双击`启动拼多多找品Agent.cmd`，确认右上角显示`MVP 5.2`。
- [ ] 重新导入或保留曾出现“TORX与内六角冲突但推荐同款95%”的任务，确认页面改为“需要复核”，显示安全闸门提示，并且不再显示“核验规格价”按钮。
- [ ] 对该任务点击“重新AI判断”；若模型仍输出顶层同款但理由或候选明细存在关键冲突，确认结果降级为`possible_match`、显示置信度不高于84，且不会自动收藏、核验或写入采购价。
- [ ] 找一件规格明确一致的正常样本，确认没有冲突的`same_product`、置信度不低于85且无需人工复核时仍可继续原有自动流程，避免安全闸门误伤正常同款。
- [ ] 如再次出现“退出码137”，确认程序自动重试而不是第一次就失败；若连续3次均失败，记录最终提示和MuMu当时状态。
- [ ] 对购买页读不到规格的商品复测，确认提示能区分可能单一规格/控件未暴露；检查`pinduoduo-agent/runtime/diagnostics`生成同一时间的JSON和PNG，且拼多多没有提交订单。

## 10. 已知回归样本汇总

| SKU | 期望绿标 | rFBS 三档 | 自动佣金 | 备注 |
|---|---|---|---|---|
| 4479204333 | 146.65 | 12% / 17% / 17% | 17%（第 2 档） | 低价 |
| 3871914086 | 894.14 | 12% / 14% / 18% | 18%（第 3 档） | 高价 |
| 4821128720 | — | — | — | 跟卖最低价 ¥122.49 |

## 11. 记录与收尾

- [ ] 把验收结果（通过 / 失败项、失败复现步骤）写进 `CHANGELOG.md` 对应版本的「验证」栏。
- [ ] 若发现 bug：先建回滚备份 → 修复 → 重新生成 zip → 重新加载 → 重新跑本清单。

> 提示：`npm test` 已覆盖解析、规则、Worker 纯逻辑；本清单重点补真实浏览器交互与 Ozon / 毛子 ERP 动态页面的验收缺口。
