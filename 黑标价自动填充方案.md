# 黑标价自动填充 - 方案与讨论纪要

> 状态：**已在扩展0.6.9实现，0.6.10修复入口误选，0.6.11改为价格出现即读取，待复测耗时与完整回填**（2026-08-25）
> 用途：给后续 agent 续接，避免重新走一遍需求澄清和 DOM 确认。
> 一句话结论：黑标价 = 商品页的"原始黑价"（优先读取`span.pdp_h0b`，并带同一`webPrice`位置兜底），按绿标价的来源分两种情况读取。

## 1. 需求目标

现在扩展只自动填「绿标价格」= `min(页面绿底价, 跟卖最低价)`，而「黑标价格」是纯手工填。

目标是把「黑标价格」也自动化，规则是：

```
绿标价格 = min(页面绿底价, 跟卖最低价)          ← 已实现
黑标价格 = 取决于绿标价来自哪一方：
  ├─ 页面绿底价 更低 → 取当前页绿底价下方那个"原始黑价"
  └─ 跟卖最低价 更低 → 进毛子 ERP 跟卖列表，找到该最低价对应的单品链接，
                       打开该单品页，取该页绿底价下方的"原始黑价"
```

关键澄清：**"黑标价格" = "原始黑价"，不是毛子算好的"黑标价"**（见 3.1）。

## 2. 已确认的业务规则

- 黑价的位置是统一的：永远是"某个商品页上、页面绿底价正下方紧挨着的那一个价格"。
- Case 1 本地读当前页；Case 2 需要跳到跟卖最低价那条的单品页再读。
- 这些数据（绿价/黑价/跟卖列表）全部是毛子 ERP 插件**客户端渲染**出来的，不在 Ozon 原始 HTML 里，因此：
  - **不能用 `fetch()` 直接抓单品页**，必须真的在浏览器里打开该页等 ERP 渲染完成后再读。
- 跟卖列表是 **hover 触发的弹窗（tooltip）**，不是展开面板；明细含店铺名 / SKU / 价格，按价格**从低到高**排序。
- 因此"跟卖最低价那条" = **弹窗里的第一行**（已按价格升序），不必再比较价格。

## 3. 已确认的 DOM 结构（关键，勿重猜）

### 3.1 页面绿底价与原始黑价（同一个 webPrice widget 内）

```html
<div data-widget="webPrice" class="pdp_b7g pdp_bh">
  <div class="pdp_gb6">
    <div class="pdp_b2g pdp_b4g pdp_gb4 ...">          <!-- 绿价块 -->
      ...
      <span class="tsHeadline600Large">122,61 ¥</span> <!-- 页面绿底价（与银行） -->
    </div>
    <div class="pdp_bh0 pdp_h3b">                      <!-- 紧邻绿价块的下一块 = "下一行" -->
      <div class="pdp_hb2">
        <div class="pdp_h2b">
          <span class="pdp_h0b tsHeadline500Medium">129,02 ¥</span> <!-- 原始黑价（与其他银行） -->
        </div>
        <div class="pdp_bh2"><span class="pdp_hb0 tsBody400Small">与其他银行</span></div>
      </div>
    </div>
  </div>
</div>
```

- 目标黑价选择器：`span.pdp_h0b`（稳定 class，数值 `129,02`）。
- ⚠️ **易混**：`pdp_h0b` = 价格数值；`pdp_hb0` = 文字"与其他银行"。别搞反。
- 毛子另注入一个 `#mz-black-price-tag`，内容是"黑标价：125.15 ¥"，这是毛子按公式算好的值，**不是我们要的**。
- 毛子公式（从 tag 上的说明文字读出，供核对）：`黑 < 绿 × 1.06 → 黑标价 = 黑 × 0.97`。代入 129.02 / 122.61 得 125.15，对得上。实现时**只读 `span.pdp_h0b`，不复制这套 ×0.97 公式**。

### 3.2 跟卖列表摘要（hover 入口）

```html
<span style="cursor: pointer; text-decoration: underline;">
  <span style="color: rgb(0, 91, 255);">Haixu X</span>等
  <span style="color: rgb(255, 77, 79);">2个</span>卖家
</span>
```

- 这是毛子 ERP 注入的可 hover 摘要；鼠标悬停才弹出明细。
- 锚点只能用**文字内容**（"跟卖列表"、"等 N 个卖家"）+ 语义结构，**不能用 `data-v-0be479e1` 这类 Vue 哈希**（ERP 每次更新就变）。
- 0.6.9真实测试暴露：不能把左侧灰色标签“跟卖列表：”本身作为候选，否则按最短文本会误选它；0.6.10改为只接受包含“等/共 N 个卖家”且可悬停或带下划线的右侧真实入口。
- 0.6.10真实测试已能取到最低价链接，但等待Ozon整页`complete`会超时；0.6.11不再等待整页完成，页面可执行脚本后立即轮询`webPrice`，读到即关闭临时页，整段上限8秒。

### 3.3 跟卖列表弹窗（Ant Design 表格）

```html
<tr class="ant-table-row ant-table-row-level-0">
  <td class="ant-table-cell"><img src="..."></td>                                          <!-- 1 头像 -->
  <td class="ant-table-cell"><a href="/seller/4770020/" target="_blank">Haixu X</a></td>    <!-- 2 店铺名 -->
  <td class="ant-table-cell"><span>中国</span></td>                                         <!-- 3 发货地 -->
  <td class="ant-table-cell">
    <a href="https://www.ozon.ru/product/...-4821128720/" target="_blank">4821128720</a>   <!-- 4 SKU = 完整单品链接 -->
  </td>
  <td class="ant-table-cell"><span>124,29 ¥</span></td>                                     <!-- 5 价格 -->
  <td class="ant-table-cell"><span style="color:#999;">0</span></td>                        <!-- 6 -->
  <td class="ant-table-cell"><span>0</span></td>                                            <!-- 7 -->
</tr>
```

- 弹窗是标准 Ant Design 表格：`tr.ant-table-row` + `td.ant-table-cell`，结构稳定。
- 第 4 列 `td` 里的 `<a href>` 就是**完整单品链接**，可直接取。
- 第一行 `tr` = 最低价那条（已按价格升序）。
- 弹窗是 hover 短暂态，AntD 表格可能异步渲染 → 需要"悬停 → 轮询等 `tr.ant-table-row` 出现 → 抓第一行第 4 列 → 移开"。

## 4. 技术方案

```
绿标价 = min(页面绿底价, 跟卖最低价)                    ← 已有，不动

黑标价：
  Case 1 页面绿底价赢
      → 读当前页 span.pdp_h0b（原始黑价）

  Case 2 跟卖最低价赢
      → 程序化 hover「跟卖列表：…等 N 个卖家」触发弹窗
      → 等 tr.ant-table-row 出现
      → 取第一行第4列 <a> 的 href（单品链接）
      → 后台/专用标签页打开该链接
      → 等该页 span.pdp_h0b 出现 → 读原始黑价 → 回填 → 关页

  任何一步读不到（无黑价 / 弹窗不出现 / 链接取不到 / 落地页无黑价）
      → 黑标价留空 + 面板提示，绝不静默填错
```

### 4.1 Case 2单一价格特例（0.6.22）

- 最低跟卖来源页可能没有绿色银行卡价和“与其他银行”价，`webPrice`内只显示一个普通黑色价格。
- 仅当可见`webPrice`内排除`#mz-black-price-tag`后只有一个唯一货币价格、未识别到绿色价格，并且连续三次稳定一致时启用。
- 该唯一价格同时作为绿标与原始黑标，并据此重新计算佣金、国际运费和18%最高采购成本。
- 价格区外的“更快”推荐和毛子计算标签不参与；存在多个无法归类的黑色价格时不猜测，继续失败留空。
- 真实示例：SKU `5594634396`来源页唯一普通价格`582.93`。

实现原则（保证不伤现有功能）：

- **fail-open**：黑价读取做成可失败的旁路，失败就留空，和现在手工留空行为一致。
- **带超时**：所有 DOM 读取都有超时，绝不阻塞"检查本页数据 / 发送到核价页"主链路。
- **后台标签页**：Case 2 的跨页读取用后台/专用标签页，不抢用户焦点、不打断当前操作。
- **可回退**：留一个开关，能一键退回"黑价手工填"。

## 5. 稳定性影响评估

- **对现有功能**：可做到基本为零影响（旁路 + 超时 + fail-open）。
- **新功能自身**：
  - 🟢 Case 1（读当前页黑价）：低风险，只是多读一个元素，失败留空。
  - 🟡 `pdp_*` / `tsHeadline*` 是 Ozon 的**混淆 class，每次发版会变**。绿价用"颜色检测"绕开，但黑价是黑字、无稳定颜色信号，只能靠 class 或位置，长期有漂移风险。
  - 🔴 Case 2（hover 弹窗 + 跨标签页）：最脆弱。弹窗是短暂态、AntD 异步渲染，还要开另一页等 ERP 渲染，任何一步卡住都可能失败，需要维护。

## 6. 已知风险与坑

- 别用 `data-v-0be479e1` 等 Vue 哈希当选择器。
- `pdp_h0b`（数值）vs `pdp_hb0`（文字）别搞混。
- 弹窗里最低价 `124,29` 与扩展现有"跟卖最低价"单值可能不完全相等（小数/汇率差异）；Case 2 判"谁赢"和"跳哪条"要对齐那个单值，避免跳错行。
- 不能 `fetch` 单品页（拿不到 ERP 渲染的黑价），必须真开浏览器页。

## 7. 待决策点（实现前需用户拍板）

0.6.9实际采用：Case 2全自动后台临时标签页；并入“检查本页数据”主流程；失败留空并提示手工填写；用户手工值优先，不被自动结果覆盖。

1. **Case 2 执行方式**：全自动（扩展自己开标签页读，会短暂抢焦点） vs 半自动（提示用户点开单品链接，扩展检测到后自动读回填）。→ 倾向**半自动起步**更稳。
2. **触发时机**：并入"检查本页数据 / 发送"流程自动做，还是加独立按钮"读取黑标价"。→ 倾向**并入主流程**，少一步点击。
3. **读不到黑价的兜底**：留空 + 面板提示。→ 已推荐，待确认。

## 8. 建议实施顺序

1. 先做 **Case 1**（页面绿底价赢时读当前页黑价）：改动小、风险低，先跑通。
2. 再做 **Case 2**（跟卖最低价赢时跨页读）：单独一个迭代，配单独验收。
3. 全程遵守 fail-open + 超时 + 后台标签页 + 可回退开关。

## 9. 记录信息（供核对）

- 例子单品链接（SKU 4821128720）：`https://www.ozon.ru/product/uplotnitel-avtomobilnyy-art-uplotnitel-dvernogo-proema-na-kuzov-dlya-great-wall-hover-4821128720/`
- 例子数值：绿底价 `122,61`；原始黑价 `129,02`；毛子"黑标价" `125,15`（= 129.02 × 0.97）；弹窗最低价 `124,29`。
- 相关代码：`ozon-erp-collector-extension/content.js`（`collectProduct` / `fillEditor` 中黑价相关逻辑）。
