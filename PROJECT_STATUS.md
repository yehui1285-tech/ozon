# PROJECT_STATUS.md - Ozon 当前项目状态

更新时间：2026-07-10
用途：给新 Codex 对话快速续接项目，不再依赖旧聊天上下文。

## 一句话状态

Ozon 核价/采集工具已完成 P0-P2 本地升级：同步安全、完整行校验、统一运费规则、自动测试/发布、网页源码拆分、裁图内存优化与 Git 初始化均已完成；等待上传网页、重载扩展并部署 Worker。

## 当前重要版本

- Chrome/Edge 扩展：0.5.7
- 在线页面入口：`https://yehui1285-tech.github.io/ozon/feishu.html?v=20260710`
- GitHub 仓库：`https://github.com/yehui1285-tech/ozon`
- 飞书同步 Worker：`https://ozon-feishu-sync.yehui1285.workers.dev/`
- Ozon 详情采集 Worker：`https://ozon-erp-collector.yehui1285.workers.dev/`

## 当前主要能力

- 从 Ozon 商品详情页读取 SKU、绿标价格、佣金、尺寸、重量、跟卖链接等信息。
- 发送到本地/线上核价页后新增一行，不覆盖上一行。
- 核价页按当前规则计算佣金、国际运费、定价、利润和利润率。
- 支持导出/导入 CSV。
- 支持同步到飞书多维表格，并按 SKU / 跟卖链接跳过重复明细。
- 支持在核价页打开飞书多维表格。
- 扩展支持手动启动、本轮自动启动、采集日志、发送前编辑确认、重复提醒、面板拖动和折叠。

## 最近已知改动

- 2026-07-10：Worker 强制来源和同步令牌校验，限制请求大小与 Ozon 域名，增加 KV 幂等缓存、去重索引重建和 `/health` 版本检查。
- 2026-07-10：网页同步前强制五项必填完整，自动保存增加 1000 行/容量保护，CSV 增加公式注入防护。
- 2026-07-10：网页与扩展运费规则统一到 `shared/freight-rules.json`，扩展升级到 0.5.7。
- 2026-07-10：网页源码拆分到 `web-src`，新增构建、测试和发布脚本；项目 Git 仓库已初始化。
- 2026-07-10：裁图工具改为低分辨率预览、下载时生成高清图，并增加批次上限。
- 2026-07-09：清理超额旧备份，并将“常规 `_备份_...` 最多保留最新 5 个”补入 `AGENTS.md` 固定规则。
- 2026-07-09：首个 Ozon 商品复用空白首行；历史仅含默认运费 `37.44` 的占位行会自动清理，不再导致商品从第 2 行开始。
- 2026-07-09：空白首行不再由运费示例自动写入 `37.44`，零值、默认系数、贴单费和负利润在开始核价前保持空白。
- 2026-07-09：核价明细实时自动保存到当前浏览器，关闭或刷新页面后会自动恢复。
- 2026-07-09：新增“清空全部记录”按钮，确认后同时覆盖本机自动保存的旧记录。
- 2026-07-06：按 Excel 运费价格测算表校准网页国际运费规则。
- 2026-07-06：修复手动系数/手动定价重置后，当前行残留旧运费的问题。
- 2026-07-06：新增“打开飞书多维表格”按钮。
- 2026-07-04：飞书同步前按 SKU / 跟卖链接去重。
- 2026-07-01：新采集行备注默认空白，手动系数改为手动输入。
- 2026-06-30：扩展 0.5.6，加快本轮自动启动注入速度。

## 当前交付状态

- 根目录 `feishu.html` 是网页上传 GitHub Pages 的目标文件。
- `ozon-feishu-sync\site\index.html` 是网页源码侧文件；网页修改时通常需要与 `feishu.html` 保持一致。
- 2026-07-10 本地两个网页文件已由 `web-src` 构建并同步；线上新功能需要上传新版 `feishu.html` 后才会生效。
- `ozon-erp-collector-extension.zip` 是扩展分发包；扩展修改后必须重新生成。
- 常规 `_备份_...` 目录按日期、模块、版本命名，最多只保留最新 5 个；`_旧文件备份_...` 独立归档不计入。

## 下一次新对话建议开场白

```text
请在 C:\Users\Microsoft\Documents\Ozon 继续 Ozon 项目。先阅读 AGENTS.md 和 PROJECT_STATUS.md，再根据我的新需求执行。不要重新扫描整个项目，除非任务需要。
```

## 当前待办

- 上传根目录新版 `feishu.html` 到 GitHub Pages。
- Cloudflare Worker 新增 `SYNC_API_TOKEN`，创建并绑定 `SYNC_CACHE` KV，确认 `ALLOWED_ORIGIN=https://yehui1285-tech.github.io`，然后部署新版 `worker.js`。
- 部署后打开 `/health` 确认版本为 `2026.07.10-p0p2`，并在网页填写同一个同步令牌。
- 在 Chrome/Edge 扩展管理页重新加载 0.5.7；分发给其他电脑时使用已重新生成的 `ozon-erp-collector-extension.zip`。
- 首次部署 KV 后，在网页点击“重建去重索引”。

## 高风险注意

- 不要把飞书 App Secret、账号密码、token、cookie 写入公开文档或 GitHub。
- 如果需要处理飞书 Worker 环境变量，只说明变量名，不在公开文件里写 secret 值。
- 如果对线上页面是否已上传不确定，明确说明“本地已准备”和“线上是否已部署”是两件事。
