# Ozon 工具另一台电脑安装与验收说明

适用版本：Ozon 扩展 `0.6.29`、拼多多找品 Agent MVP `5.1`。本说明用于让另一台 Windows 电脑使用当前已经实现的网页核价、Ozon 采集/扫描扩展、任务核价补全和 MuMu 拼多多找品能力。

## 先明确交付边界

- Git 仓库提供项目代码、使用文档、扩展源码和当前发布 ZIP；新电脑应以克隆仓库为准，不要只发送单个 CMD 文件。
- `启动拼多多找品Agent.cmd` 只是启动器，必须和 `pinduoduo-agent` 整个目录、Node.js、MuMu、拼多多 App 一起使用。
- 不交付或复制拼多多登录态、浏览器 Cookie、阿里云百炼 API Key、飞书/Cloudflare 密钥、代理订阅链接，以及 `pinduoduo-agent/runtime`。
- 浏览器本地队列、候选截图和未提交的任务文件不会通过 Git 自动同步；如需交接业务任务，只导出/导入脱敏的任务 JSON。

## 交付清单

| 项目 | 获取方式 | 用途 |
| --- | --- | --- |
| Ozon 项目 | 克隆 `https://github.com/yehui1285-tech/ozon.git` | 源码、文档、脚本和当前扩展发布包 |
| Git、Node.js | 在新电脑分别安装 | 获取代码、运行本地 Agent、执行构建/测试 |
| Chrome 或 Edge | 在新电脑安装 | 加载 Ozon 扩展、使用在线核价页 |
| MuMu 模拟器 | 在新电脑安装 | 运行拼多多 App 自动化 |
| 拼多多 App | 在 MuMu 内自行安装并登录 | 以图找同款、收藏、规格实价核验 |
| 百炼 API Key（可选） | 使用方自行创建/配置 | AI 同款判断；没有 Key 仍可使用非 AI 找款流程 |

## 一、新电脑取得项目

1. 安装 Git 后打开 PowerShell，执行：

   ```powershell
   Set-Location C:\Users\Microsoft\Documents
   git clone https://github.com/yehui1285-tech/ozon.git Ozon
   Set-Location .\Ozon
   git status
   git log -1 --oneline
   ```

2. 在`git log --oneline`中确认包含提交 `8d4f5b8`（拼多多 MVP 5.1）；后续仅更新文档的提交在其上方属于正常。私有仓库若要求授权，请在 GitHub 浏览器登录流程中完成；不要把密码或 Token 写入命令、文档或项目文件。
3. 若旧电脑有需要延续的任务，只复制明确的导出 JSON 到新电脑后再导入。不要覆盖新电脑的整个 `pinduoduo-agent/runtime`。

## 二、安装 Ozon 浏览器扩展

1. 在项目根目录找到 `ozon-erp-collector-extension.zip`，解压到固定目录，例如：

   ```text
   C:\Users\Microsoft\Documents\Ozon\ozon-erp-collector-extension
   ```

   克隆后的该目录已存在；也可以直接使用该目录，不必再次解压 ZIP。
2. Chrome 打开 `chrome://extensions/`，或 Edge 打开 `edge://extensions/`。
3. 开启“开发者模式” → “加载已解压的扩展程序” → 选择 `ozon-erp-collector-extension` 文件夹。
4. 确认扩展名称为 `OZON ERP Detail Sender`，版本为 `0.6.29`。以后项目升级后，回到此页点击“重新加载”。

扩展负责 Ozon 商品采集、店铺/批量扫描、主图与核价补全；拼多多 Agent 的“条件式 Ozon 最终复价”也依赖已加载的 `0.6.29` 扩展。

## 三、安装并启动拼多多找品 Agent

1. 安装 Node.js LTS，重新打开 PowerShell 后运行：

   ```powershell
   node -v
   ```

   有版本号即可。本 Agent 当前不依赖单独的 `npm install` 步骤。
2. 安装 MuMu 模拟器。当前 Agent 默认使用以下路径：

   ```text
   E:\Program Files\Netease\MuMu\nx_main\MuMuManager.exe
   E:\Program Files\Netease\MuMu\nx_main\adb.exe
   ```

   请优先按此位置安装；若新电脑只能安装到其他路径，先不要修改文件，记录实际安装路径后再由维护者调整 Agent 配置。
3. 在 MuMu 内自行安装拼多多 App，并由实际使用者登录自己的账号。不要复制旧电脑的 App 数据、浏览器登录态或账号 Cookie。
4. 双击项目根目录的 `启动拼多多找品Agent.cmd`。浏览器打开：

   ```text
   http://127.0.0.1:17628/
   ```

5. 点击“启动模拟器”（如需要）并等待安卓桌面出现，再点击“检查模拟器”。只有 MuMu 已启动、拼多多已安装时，才可开始找款。

## 四、配置 AI 同款判断（可选）

1. 双击 `配置千问API密钥.cmd`，由新电脑的使用者输入自己的百炼 API Key。
2. 密钥会使用该 Windows 账户的 DPAPI 加密保存到 `pinduoduo-agent/runtime`。该目录不可迁移、不会提交 Git。
3. 重新打开 Agent，右上角显示 `qwen3.7-flash已配置` 后才可使用“AI判断”或“批量AI判断同款”。

不配置 Key 时，仍可使用 Ozon 核价补全、拼多多以图搜索、候选详情读取和人工判断；AI 推荐、自动收藏和自动规格判断不可用。

## 五、首次验收（必须完成）

### 扩展与 Ozon

- [ ] 打开一个 Ozon 商品页，扩展能显示采集面板并读取当前 SKU、绿标价、佣金、尺寸和重量。
- [ ] 从扩展打开“批量店铺扫描”，确认可创建任务、暂停/继续和导出。
- [ ] 导入一份找品任务 JSON 到“任务批量核价补全”，确认可读取主图和核价字段。

### MuMu 与拼多多 Agent

- [ ] Agent 的“检查模拟器”显示 MuMu 已启动、拼多多已安装。
- [ ] 导入一份已完成 Ozon 主图与核价的任务 JSON，执行一件“找同款”。
- [ ] 确认候选标题、展示价和稳定商品链接能显示；低网速时已取得商品 ID 的候选应保留链接并标明缺失字段。
- [ ] 如已配置 AI Key，执行一次“AI判断”，确认有置信度与人工复核结论。
- [ ] 使用一件目标规格商品，确认程序只读取规格常规价、不点击“提交订单”。
- [ ] 使用一件规格价低于历史上限的商品，确认 Ozon 最终复价可以执行；重复操作 30 分钟内应复用缓存。

### 网页与同步

- [ ] 打开 `https://yehui1285-tech.github.io/ozon/feishu.html?v=20260720`，确认核价计算可用。
- [ ] 如需飞书同步，使用方在网页按既有安全配置操作；不要在本地项目录入或保存飞书密钥。

完整扩展回归项目见 `真实浏览器验收清单.md`；本说明的验收仅用于确认新电脑的环境能够接手工作。

## 六、日常使用顺序

1. Ozon 扩展批量扫描店铺，导出找品任务 JSON。
2. 在扩展“任务批量核价补全”中补齐主图和实时核价，下载结果 JSON。
3. 启动 MuMu 与拼多多 Agent，导入该 JSON，执行“批量自动找同款”。
4. 需要时执行 AI 判断；低置信度、规格冲突、运费未知或详情不完整的商品保持人工复核。
5. 确认规格常规价后，才接受条件式 Ozon 最终复价结论；程序不得使用余额、券后下单价或多件门槛优惠作为采购成本。

## 七、常见问题

| 现象 | 处理 |
| --- | --- |
| 双击 Agent 后提示 `node` 不存在 | 安装 Node.js 后重新打开 CMD/PowerShell，确认 `node -v`。 |
| 提示找不到 MuMu 控制器或 ADB | 核对 MuMu 是否安装在默认 E 盘路径；路径不同需调整 Agent 配置。 |
| “检查模拟器”失败 | 先启动 MuMu，等系统完全启动后重试；确认拼多多 App 已安装。 |
| 最终复价提示桥接不可用 | 在扩展页重新加载 `0.6.29`，刷新 Agent 页面后重试。 |
| AI 功能未配置 | 在新电脑重新运行 `配置千问API密钥.cmd`，不要复制旧电脑运行目录。 |
| 任务/截图不见了 | 这些是本机数据；从旧电脑导出任务 JSON 后再导入，不依赖 Git 同步。 |

## 八、交付安全规则

- 不共享账号、密码、Cookie、浏览器配置文件、百炼 API Key、飞书/Cloudflare 密钥、代理订阅或 `runtime` 目录。
- 每台电脑使用独立的拼多多登录和 API Key；不要让两台电脑同时控制同一个 MuMu 实例或拼多多账号。
- 新电脑通过全部首次验收前，保留旧电脑的项目和运行环境。
- 代码更新时，在项目目录执行 `git pull --ff-only`，然后重新加载扩展；涉及扩展新版本时按项目发布说明重新生成/替换 ZIP。
