# 1688 主渠道采购找品设计

日期：2026-08-31
状态：设计已确认，等待用户复核后进入实施计划

## 1. 背景

当前找品流程依赖 MuMu 模拟器中的拼多多 App。MVP 5.3 已能完成以图搜索、候选详情读取、Qwen 同款判断、目标规格核价和 Ozon 最终复价，但模拟器、App 页面、账号状态和连续批量访问会带来页面失效、售罄回退、界面读取失败和较长耗时。

本次调整将 1688 网页渠道设为默认找款来源，保留 MuMu 拼多多作为人工启用的备用渠道。现有 AI 一致性安全闸门、规格核验、逐件保存和 Ozon 18% 利润复价继续复用。

## 2. 目标

- 以 1688 官方采购助手和 1688 正常网页为默认找款入口。
- 自动读取候选、商品详情、目标规格价格、起订量和国内运费。
- 由 Qwen 自动判断同款和目标规格，满足安全条件时自动回填链接与单件采购总成本。
- 无法可靠确认的任务转人工，不猜测价格，不自动下单。
- 保留现有拼多多 MVP 5.3，作为单件手动启用的备用渠道。
- 将平台相关逻辑隔离为渠道适配器，便于以后增加其他采购平台。

## 3. 非目标

- 不自动下单、付款、联系客服或发送旺旺消息。
- 不绕过 1688 登录、验证码、账号验证或平台访问限制。
- 不使用来源不明的非官方商品数据接口。
- 第一阶段不删除、重命名或大规模迁移 `pinduoduo-agent` 目录。
- 第一阶段不自动将无单件证据的两件起批价当作一件采购价。

## 4. 总体架构

```text
Ozon任务与主图
      ↓
采购找品编排层
      ↓
找款渠道适配层
 ├─ 1688浏览器渠道（默认）
 └─ MuMu拼多多渠道（备用）
      ↓
统一候选商品格式
      ↓
Qwen同款与目标规格判断
      ↓
确定性安全闸门
      ↓
自动回填或人工复核
      ↓
Ozon最终18%利润复价
```

### 4.1 Chrome/Edge 扩展

- 在用户已登录的浏览器中识别 1688 搜索结果页和商品详情页。
- 读取页面可见的候选、SKU、起订量、单件/拿样价、阶梯价、运费和供应商信息。
- 通过受限的本地桥接把结构化数据交给本地 Agent。
- 只访问明确列入白名单的 1688 页面，不读取无关网页。

### 4.2 本地采购找品 Agent

- 继续使用现有 `pinduoduo-agent` 目录，页面名称改为“采购找品 Agent”。
- 新增渠道编排和候选标准化，保留旧拼多多接口作为适配器。
- 负责任务状态、Qwen 调用、安全闸门、逐件保存、耗时记录和失败诊断。

### 4.3 1688 官方采购助手

- 负责官方以图找货入口和结果呈现。
- 第一阶段允许一次用户发起动作，不要求人工挑选候选。
- 用户发起图片搜索后，扩展检测其打开的正常 1688 结果页，后续候选读取、AI 判断和回填自动继续。
- 实施前先做只读兼容性探针，确认结果页 URL、DOM 和详情页能被当前扩展合法读取。若官方助手结果不落在可访问的正常 1688 页面，则改用 1688 自身可见图片上传入口；两种入口均不可用时停止实施，不采用跨扩展注入或非官方抓包绕行。

## 5. 渠道接口与统一候选格式

编排层只依赖统一接口，不直接依赖 MuMu 或 1688 页面结构：

- `prepareSearch(task)`：准备主图和任务上下文。
- `startSearch(task)`：启动或等待渠道搜索结果。
- `collectCandidates(task)`：读取轻量候选列表。
- `inspectCandidate(candidate)`：读取候选详情。
- `readSkuOptions(candidate)`：读取规格、价格、起订量和运费。
- `captureEvidence(context)`：保存可回查证据。

统一候选至少包含：

- `provider`、`candidateId`、`productId`、`sourceUrl`
- 标题、主图、属性和供应商名称
- 展示价、单件价、拿样价、阶梯价及其来源
- `minimumOrderQuantity`
- 是否支持一件代发或拿样
- 国内运费及其读取状态
- SKU 维度、选项、选中回执和选项实际价格
- 详情完整性、失败原因、证据截图和采集时间

现有拼多多候选在进入 AI 判断前也转换为该格式，避免前端和 Qwen 继续依赖平台专属字段。

## 6. 自动找款数据流

1. 用户启动单件或批量 1688 找款。
2. Agent 准备 Ozon 主图并进入 `waiting_search_results`。
3. 用户通过官方采购助手发起图片搜索；扩展识别结果页后自动接管。
4. 读取当前结果前 12 个轻量候选。
5. 排除商品失效、无有效价格、类目明显不匹配或起订量大于 2 的候选。
6. 按视觉相似度、标题和采购约束排序，优先核验前 5 个详情。
7. 将完整候选交给 Qwen 判断同款和目标规格。
8. 对 AI 选择执行确定性复核：候选存在、规格存在、页面选中回执有效、价格前后一致、起订量和运费满足规则。
9. 安全闸门通过后回填商品链接、目标规格和采购成本，并触发现有 Ozon 最终复价。
10. 任一条件不满足则保存候选与原因，转人工复核。

## 7. 起订量和单件价格规则

- 起订量大于 2：自动排除。
- 起订量等于 2：保留为候选并标记 `negotiable_single_unit`。
- 页面明确提供一件代发价或拿样价：可按该单件价格继续自动核验。
- 页面只有两件起批价：不得直接当作一件采购价，转人工确认。
- 人工确认某个商品或供应商支持一件购买后，保存带来源和时间的本地例外记录；仅对同一商品或明确相同报价规则复用，不扩散到该供应商全部商品。
- 起订量等于 1：按目标 SKU 的实际单件价继续。

## 8. AI 判断与安全闸门

Qwen 负责判断，不负责生成价格。自动回填必须同时满足：

- 顶层结论为 `same_product`。
- 置信度不低于 85%。
- 最佳候选与逐候选结论、关键差异和判断理由一致。
- 目标规格明确，且页面中存在对应选项。
- 页面选中回执与 AI 目标规格一致。
- 规格价格来自页面实际读取，重新确认后价格不变。
- 支持购买一件，或存在已验证的一件代发/拿样价/本地例外记录。
- 国内运费已明确读取；包邮按 0 元处理。
- 价格不依赖首单、会员、优惠券、多件门槛或其他不可稳定复现的优惠。

多维规格组合、型号/数量冲突、AI 结论矛盾、低置信度、运费未知或价格变化均转人工。

## 9. 采购成本计算

自动写入的采购成本为：

```text
目标规格单件实际价格 + 本次购买一件对应的国内运费
```

页面最低展示价、两件起批单价、首单价、会员价、优惠券价和多件优惠不直接作为采购成本。采购成本通过安全闸门后，再进入现有 Ozon 实时价格、佣金、国际运费和 18% 利润率复价流程。

页面分别展示商品单价、国内运费和单件采购总成本，三者不得混写。

## 10. 状态与恢复

新增通用状态：

- `waiting_search_results`
- `collecting_candidates`
- `inspecting_candidate_details`
- `ai_judging`
- `verifying_target_sku`
- `auto_filled`
- `pending_human_review`
- `paused_platform_verification`
- `no_suitable_candidate`
- `search_failed_retryable`

每个阶段完成后立即持久化。刷新、浏览器重启或 Agent 中断后，从当前任务的安全阶段继续，不清空已取得候选。旧的拼多多任务状态继续兼容。

## 11. 异常处理

- 1688 未登录、验证码或账号验证：暂停整批，保留当前任务等待人工处理。
- 官方助手未打开结果页：保持 `waiting_search_results`，允许重新发起。
- 单个商品失效：跳过该候选，继续核验后续候选。
- 页面字段缺失或结构变化：保存页面诊断和截图，不猜测缺失字段。
- 目标规格不存在、选中回执失败或价格变化：停止回填并转人工。
- 起订量为 2 且没有单件证据：显示“需确认单件采购价”。
- 1688 无合适候选：显示“用拼多多备用找款”按钮，不自动启动 MuMu。
- 平台失败不会删除其他任务或覆盖已有人工确认结果。

## 12. 页面调整

- 标题改为“采购找品 Agent”。
- 默认渠道显示为“1688”，每行可切换“拼多多备用”。
- 候选卡片显示平台、供应商、起订量、目标规格、价格来源和运费状态。
- 结果区分“商品单价、国内运费、单件采购总成本”。
- 显示 AI 置信度、关键差异、安全闸门结果和人工复核原因。
- 批量面板继续显示逐件耗时，并按搜索、候选详情、AI、规格核验拆分阶段。

## 13. 安全与数据边界

- 不保存 1688 密码、Cookie 或账号验证码。
- 浏览器登录态保留在用户浏览器中，不写入任务 JSON 或 GitHub。
- Qwen 密钥继续使用环境变量或本机 DPAPI 加密文件。
- 运行证据、页面诊断和任务历史继续保存在已忽略的本地运行目录。
- 公开文档、提交和发布 ZIP 不包含账号凭据、Cookie、任务导出或供应商私聊内容。

## 14. 测试与验收

### 14.1 自动测试

- 1688 页面样本解析和候选标准化。
- 起订量大于 2 的排除规则。
- 起订量等于 2 的保留、拿样价和人工确认规则。
- 阶梯价、首单价、优惠价和单件价区分。
- 商品单价、国内运费和采购总成本计算。
- AI 结论一致性、置信度和规格安全闸门。
- 状态持久化和中断恢复。
- 拼多多 MVP 5.3 回归测试。

### 14.2 真实浏览器验收

至少使用 20 件代表商品，覆盖明确同款、无同款、单一起订、两件起订、拿样价、无单件价、包邮、明确运费、运费未知、单规格、多颜色、多规格组合、商品失效、登录验证和页面读取失败。

通过标准：

- 20 件均保留明确结果或失败原因，不静默丢任务。
- 自动回填样本零误填；不确定时转人工。
- 自动回填的链接、目标规格、页面价格和运费均可回查。
- 全程不点击下单。
- 完整自动阶段平均耗时目标不超过 2.5 分钟；等待用户发起官方助手搜索和平台验证的时间单独统计。
- 旧拼多多备用流程仍可运行。

## 15. 发布与回滚

- 实施前建立包含日期和模块名的回滚备份，并将常规备份保持在最多 5 个。
- 修改扩展后运行 `tools\build-release.ps1`，重新生成 `ozon-erp-collector-extension.zip`。
- 需要在 Chrome/Edge 扩展管理页重新加载扩展并完成真实浏览器验收。
- 本次设计不修改 `feishu.html` 和 Cloudflare Worker；实施若保持该边界，则无需上传网页或部署 Worker。
- 实施完成后更新 `PROJECT_STATUS.md`、`CHANGELOG.md` 和真实浏览器验收清单，只提交确认文件，推送 `origin/main` 并核对远端 `main` 与本地 `HEAD` 一致。
